/** Alias of `any` for lit-plugin. We should use `string` for role type because role type can be extended. */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type AnyRole = any;
