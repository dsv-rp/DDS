# Build and deploy Storybook every time the main branch is updated.
# This Storybook is a development environment, as the main branch may contain unreleased content.

name: Deploy Storybook (dev)

on:
  push:
    branches:
      - main

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy-dev:
    environment:
      name: github-pages
      url: ${{ steps.build-deploy.outputs.page_url }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      # This action does "npm ci" automatically
      - name: Build Storybook and Deploy to GitHub Pages
        id: build-deploy
        uses: bitovi/github-actions-storybook-to-github-pages@v1.0.3
        with:
          build_command: npm run build && STORYBOOK_ENV=production npm run storybook:build-wc
          path: storybook-static/web-components
          checkout: false
